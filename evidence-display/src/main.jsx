import React from 'react'
import ReactDOM from 'react-dom/client'
import EvidenceDisplay from './App.jsx'
import 'antd/dist/reset.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EvidenceDisplay />
  </React.StrictMode>
)
